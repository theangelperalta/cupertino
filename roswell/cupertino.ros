#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:cupertino))
  )

(defpackage :ros.script.cupertino.3976633826
  (:use :cl))
(in-package :ros.script.cupertino.3976633826)

(defun main (&rest argv)
  (declare (ignorable argv))
  (cupertino:main argv))
;;; vim: set ft=lisp lisp:
